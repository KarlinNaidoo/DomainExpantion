# Security

- Never commit `.env`, API keys, or session transcripts.
- Keys live in a gitignored `.env` or the process environment. Create them at https://console.x.ai
- The supervisor must not send, push, deploy, or trade. Those actions wait for a live specialist plus human approval.
- Report a secret leak by rotating the key on the console and opening a private note to the repo owner.
